# Vocabulary Training Report

**Generated:** 2026-04-25 19:04:47

## Vocabulary Information

- **Size:** 26,613 tokens
- **Algorithm:** BPE
- **Created:** 2026-04-25 19:04:46
- **File:** `vocab_bpe_80k.json`

## Special Tokens

- `<PAD>`
- `<SOS>`
- `<EOS>`
- `<UNK>`
- `<MASK>`

## Statistics

## Vocabulary Size Comparison

| Model | Vocabulary Size |
|-------|----------------|
| **KuiperAI (This)** | **26,613** |
| GPT-2 | 50,257 |
| GPT-3 | 50,257 |
| BERT | 30,522 |
| LLaMA | 32,000 |
| GPT-4 | ~100,000 |

## Usage

### Loading the Tokenizer

```python
from tokenizers import Tokenizer

# Load tokenizer
tokenizer = Tokenizer.from_file("checkpoints/tokenizer_bpe_80k.json")

# Encode text
text = "Hello, world!"
encoding = tokenizer.encode(text)
print(encoding.tokens)

# Decode tokens
decoded = tokenizer.decode(encoding.ids)
print(decoded)
```

### Integration with Model

```python
import json
from tokenizers import Tokenizer

# Load vocabulary
with open("vocab_bpe_80k.json", 'r') as f:
    vocab_data = json.load(f)
    vocab = vocab_data['vocab']

# Load tokenizer
tokenizer = Tokenizer.from_file("tokenizer_bpe_80k.json")

# Update model
model = Transformer(
    vocab_size=len(vocab),
    d_model=512,
    num_heads=8,
    num_layers=6
)
```

## Next Steps

1. ✅ Vocabulary trained successfully
2. 🔄 Retrain model with new vocabulary
3. 🔄 Test generation quality
4. 🔄 Fine-tune on specific tasks

---

*This report was automatically generated by the vocabulary training pipeline.*
