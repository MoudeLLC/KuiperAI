Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks. In contrast, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
Q: Explain this in detail.
A: Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
RMSprop addresses AdaGrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. This maintains adaptive per-parameter learning rates while preventing excessive decrease over time. In contrast, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Q: Explain this in detail.
A: Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification. In contrast, weight decay implements l2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. this encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
Q: Explain this in detail.
A: Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
AdaGrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. However, the monotonically decreasing learning rate can cause premature convergence. In contrast, the universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. The noise scale typically decreases over time, allowing convergence to good solutions. In contrast, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
AdaGrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. However, the monotonically decreasing learning rate can cause premature convergence.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. This prevents instability from large updates when parameters are randomly initialized and gradients are unreliable. In contrast, the softmax temperature parameter controls the entropy of the output distribution. high temperature produces uniform distributions, encouraging exploration. low temperature creates peaked distributions, favoring exploitation. temperature annealing balances these during training.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
RMSprop addresses AdaGrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. This maintains adaptive per-parameter learning rates while preventing excessive decrease over time. In contrast, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs. In contrast, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule. In contrast, the receptive field of a neuron in a convolutional network defines the input region affecting its activation. deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. In contrast, gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. this enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. The noise scale typically decreases over time, allowing convergence to good solutions. In contrast, gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. this enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. In contrast, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. In contrast, dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. at test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. In contrast, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
RMSprop addresses AdaGrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. This maintains adaptive per-parameter learning rates while preventing excessive decrease over time. In contrast, batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. it also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Q: Explain this in detail.
A: Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. In contrast, dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. at test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification. In contrast, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
Learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. This prevents instability from large updates when parameters are randomly initialized and gradients are unreliable. In contrast, gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. this enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
AdaGrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. However, the monotonically decreasing learning rate can cause premature convergence. In contrast, stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. the momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. In contrast, the dying relu problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. this happens when large negative biases develop. leaky relu and parametric relu variants maintain small gradients for negative inputs, mitigating this issue.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. In contrast, the dying relu problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. this happens when large negative biases develop. leaky relu and parametric relu variants maintain small gradients for negative inputs, mitigating this issue.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability. In contrast, the softmax temperature parameter controls the entropy of the output distribution. high temperature produces uniform distributions, encouraging exploration. low temperature creates peaked distributions, favoring exploitation. temperature annealing balances these during training.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Second-order optimization methods like Newton's method use curvature information from the Hessian matrix to determine optimal step sizes and directions. While theoretically superior, computational cost limits practical application to small-scale problems. In contrast, the softmax temperature parameter controls the entropy of the output distribution. high temperature produces uniform distributions, encouraging exploration. low temperature creates peaked distributions, favoring exploitation. temperature annealing balances these during training.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
Q: Explain this in detail.
A: Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. The noise scale typically decreases over time, allowing convergence to good solutions. In contrast, the dying relu problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. this happens when large negative biases develop. leaky relu and parametric relu variants maintain small gradients for negative inputs, mitigating this issue.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. In contrast, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. In contrast, the learning rate schedule determines how the step size changes during training. common strategies include step decay, exponential decay, and cosine annealing. proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs. In contrast, the dying relu problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. this happens when large negative biases develop. leaky relu and parametric relu variants maintain small gradients for negative inputs, mitigating this issue.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. This prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability. In contrast, dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. at test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
AdaGrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. However, the monotonically decreasing learning rate can cause premature convergence. In contrast, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. In contrast, stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. the momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. In contrast, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
Gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. The noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. In contrast, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. In contrast, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification. In contrast, the receptive field of a neuron in a convolutional network defines the input region affecting its activation. deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule. In contrast, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. In contrast, gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. this enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. In contrast, the softmax temperature parameter controls the entropy of the output distribution. high temperature produces uniform distributions, encouraging exploration. low temperature creates peaked distributions, favoring exploitation. temperature annealing balances these during training.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. This prevents instability from large updates when parameters are randomly initialized and gradients are unreliable. In contrast, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability. In contrast, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs. In contrast, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
RMSprop addresses AdaGrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. This maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Second-order optimization methods like Newton's method use curvature information from the Hessian matrix to determine optimal step sizes and directions. While theoretically superior, computational cost limits practical application to small-scale problems. In contrast, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. In contrast, stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. the momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
Learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. This prevents instability from large updates when parameters are randomly initialized and gradients are unreliable. In contrast, the dying relu problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. this happens when large negative biases develop. leaky relu and parametric relu variants maintain small gradients for negative inputs, mitigating this issue.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. In contrast, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Second-order optimization methods like Newton's method use curvature information from the Hessian matrix to determine optimal step sizes and directions. While theoretically superior, computational cost limits practical application to small-scale problems.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
Q: Explain this in detail.
A: ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. The noise scale typically decreases over time, allowing convergence to good solutions. In contrast, the learning rate schedule determines how the step size changes during training. common strategies include step decay, exponential decay, and cosine annealing. proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification. In contrast, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
AdaGrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. However, the monotonically decreasing learning rate can cause premature convergence. In contrast, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability. In contrast, the dying relu problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. this happens when large negative biases develop. leaky relu and parametric relu variants maintain small gradients for negative inputs, mitigating this issue.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. In contrast, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. The noise scale typically decreases over time, allowing convergence to good solutions. In contrast, the dying relu problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. this happens when large negative biases develop. leaky relu and parametric relu variants maintain small gradients for negative inputs, mitigating this issue.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. In contrast, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. In contrast, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Q: Explain this in detail.
A: Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: Explain the concept of perplexity in language modeling.
A: Perplexity measures how well a probability model predicts a sample, calculated as the exponential of the average negative log-likelihood. Lower perplexity indicates better prediction. It represents the effective vocabulary size the model is uncertain about at each step.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. In contrast, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. In contrast, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. In contrast, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Explain the bias-variance decomposition of prediction error.
A: Total error decomposes into bias squared plus variance plus irreducible error. Bias measures systematic prediction error from model assumptions. Variance measures sensitivity to training data fluctuations. The tradeoff requires balancing model complexity against generalization.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks. In contrast, the receptive field of a neuron in a convolutional network defines the input region affecting its activation. deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. In contrast, dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. at test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, rmsprop addresses adagrad's diminishing learning rates by using an exponentially decaying average of squared gradients instead of cumulative sum. this maintains adaptive per-parameter learning rates while preventing excessive decrease over time.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
Gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. The noise scale typically decreases over time, allowing convergence to good solutions. In contrast, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: What causes the exploding gradient problem and how does gradient clipping solve it?
A: Exploding gradients occur when the product of gradients across layers grows exponentially, typically when weight matrices have eigenvalues greater than one. Gradient clipping rescales gradients exceeding a threshold, preventing parameter updates from becoming too large and destabilizing training.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule. In contrast, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
Q: Explain this in detail.
A: The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Q: Explain this in detail.
A: The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. In contrast, the universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. In contrast, dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. at test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Q: Explain this in detail.
A: BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Furthermore, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Furthermore, gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. the noise scale typically decreases over time, allowing convergence to good solutions.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule. In contrast, stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. the momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. The noise scale typically decreases over time, allowing convergence to good solutions. In contrast, the learning rate schedule determines how the step size changes during training. common strategies include step decay, exponential decay, and cosine annealing. proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. In contrast, weight decay implements l2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. this encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
Gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. The noise scale typically decreases over time, allowing convergence to good solutions. In contrast, dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. at test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
Q: Explain this in detail.
A: The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Q: Explain the concept of attention scores in self-attention mechanisms.
A: Attention scores measure relevance between query and key vectors using dot products, scaled by the square root of dimension to prevent saturation. Softmax converts scores to probabilities, which weight value vectors. This allows dynamic focus on relevant input positions.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. In contrast, the softmax temperature parameter controls the entropy of the output distribution. high temperature produces uniform distributions, encouraging exploration. low temperature creates peaked distributions, favoring exploitation. temperature annealing balances these during training.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Q: Explain this in detail.
A: GPT architecture uses unidirectional transformers with causal masking, predicting each token from previous tokens only. This autoregressive approach enables coherent text generation and few-shot learning through prompt engineering and in-context learning.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. In contrast, the dying relu problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. this happens when large negative biases develop. leaky relu and parametric relu variants maintain small gradients for negative inputs, mitigating this issue.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: Describe the mathematical relationship between cross-entropy and KL divergence.
A: Cross-entropy H(p,q) equals the sum of entropy H(p) and KL divergence D_KL(p||q). Minimizing cross-entropy is equivalent to minimizing KL divergence when the true distribution p is fixed, which is why cross-entropy serves as an effective loss function for classification.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
Q: Explain this in detail.
A: The sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. Its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Second-order optimization methods like Newton's method use curvature information from the Hessian matrix to determine optimal step sizes and directions. While theoretically superior, computational cost limits practical application to small-scale problems. In contrast, adagrad adapts learning rates for each parameter based on historical gradient magnitudes, using larger updates for infrequent features and smaller updates for frequent ones. however, the monotonically decreasing learning rate can cause premature convergence.
Q: Explain this in detail.
A: Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: How do generative adversarial networks achieve equilibrium?
A: GANs train a generator and discriminator in a minimax game. The generator learns to produce realistic samples while the discriminator learns to distinguish real from fake. Nash equilibrium occurs when the generator produces perfect samples and the discriminator cannot distinguish them.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, the cross-entropy loss for classification l = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. it provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Q: How does the Adam optimizer combine momentum and RMSprop?
A: Adam maintains exponential moving averages of both gradients (momentum) and squared gradients (RMSprop). It uses bias correction to account for initialization at zero, then updates parameters using the ratio of momentum to the square root of the second moment, providing adaptive learning rates per parameter.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Furthermore, learning rate warmup gradually increases the learning rate from a small value to the target value over initial training steps. this prevents instability from large updates when parameters are randomly initialized and gradients are unreliable.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. In contrast, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks. In contrast, the softmax temperature parameter controls the entropy of the output distribution. high temperature produces uniform distributions, encouraging exploration. low temperature creates peaked distributions, favoring exploitation. temperature annealing balances these during training.
ResNet architecture introduces skip connections that add the input of a residual block to its output, creating identity mappings. This enables training of networks exceeding 1000 layers by providing direct gradient paths and preventing degradation from increased depth.
Q: Explain this in detail.
A: Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
Q: Explain this in detail.
A: Vision transformers apply the transformer architecture to images by splitting them into patches, treating each patch as a token. Positional embeddings encode spatial relationships. This approach achieves state-of-the-art performance on image classification when pre-trained on large datasets.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
Q: Explain this in detail.
A: The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: Explain the mathematical foundation of backpropagation using the chain rule.
A: Backpropagation computes partial derivatives of the loss L with respect to weights w using the chain rule: dL/dw = dL/dy * dy/dz * dz/dw, where y is the output, z is the pre-activation. This recursive application enables efficient gradient computation through arbitrary network depths.
Q: Explain this in detail.
A: The Jacobian matrix contains all first-order partial derivatives of a vector-valued function. In backpropagation, Jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Furthermore, the gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. the learning rate controls step size, requiring careful tuning to balance convergence speed and stability.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Q: Explain this in detail.
A: Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability.
The learning rate schedule determines how the step size changes during training. Common strategies include step decay, exponential decay, and cosine annealing. Proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: Describe the difference between greedy decoding and beam search.
A: Greedy decoding selects the highest probability token at each step, which is fast but can miss better overall sequences. Beam search maintains multiple hypotheses, exploring top-k candidates at each step, finding higher quality sequences at the cost of increased computation.
Q: Explain this in detail.
A: The dying ReLU problem occurs when neurons output zero for all inputs, preventing gradient flow and learning. This happens when large negative biases develop. Leaky ReLU and parametric ReLU variants maintain small gradients for negative inputs, mitigating this issue.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns. Q: What is the purpose of positional encoding in transformers?
A: Since transformers process all positions in parallel without inherent sequence order, positional encodings inject position information using sinusoidal functions of different frequencies. This allows the model to utilize sequence order while maintaining parallelization benefits.
BERT uses bidirectional transformers with masked language modeling, randomly masking input tokens and predicting them from context. This pre-training objective enables learning rich contextual representations that transfer effectively to downstream tasks through fine-tuning. Furthermore, the sigmoid function sigma(x) = 1/(1+exp(-x)) maps real numbers to (0,1), providing smooth non-linearity. its derivative sigma'(x) = sigma(x)(1-sigma(x)) enables efficient backpropagation but suffers from vanishing gradients for extreme inputs.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: What is catastrophic forgetting in neural networks?
A: Catastrophic forgetting occurs when training on new tasks causes dramatic performance degradation on previously learned tasks. The network overwrites useful representations. Solutions include elastic weight consolidation, progressive neural networks, and experience replay.
The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
The receptive field of a neuron in a convolutional network defines the input region affecting its activation. Deeper layers have larger receptive fields through composition of local operations, enabling hierarchical feature learning from local to global patterns.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Q: Explain this in detail.
A: The ReLU activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. Its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. Q: How do transformers achieve parallelization compared to RNNs?
A: Transformers process all sequence positions simultaneously using self-attention, computing relationships between all pairs in parallel. RNNs must process sequentially due to hidden state dependencies, making transformers significantly faster on modern hardware with parallel processing capabilities.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability. In contrast, the universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons.
Q: Explain this in detail.
A: Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation.
Q: Explain this in detail.
A: The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
The softmax temperature parameter controls the entropy of the output distribution. High temperature produces uniform distributions, encouraging exploration. Low temperature creates peaked distributions, favoring exploitation. Temperature annealing balances these during training. In contrast, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Q: Explain this in detail.
A: Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Stochastic gradient descent with momentum accumulates exponentially decaying moving averages of past gradients, accelerating convergence in relevant directions while dampening oscillations. The momentum coefficient typically ranges from 0.9 to 0.99, balancing responsiveness and stability.
The cross-entropy loss for classification L = -sum(y_true * log(y_pred)) measures the difference between true and predicted probability distributions. It provides strong gradients for incorrect predictions, accelerating learning compared to squared error for classification.
Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Gradient noise injection adds random noise to gradients during training, helping escape sharp local minima and improving generalization. The noise scale typically decreases over time, allowing convergence to good solutions. In contrast, dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. at test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Furthermore, the relu activation function f(x) = max(0,x) provides non-linearity while maintaining gradient flow for positive inputs. its simplicity enables fast computation and mitigates vanishing gradients, making it the default choice for hidden layers in deep networks.
Q: Explain this in detail.
A: The encoder-decoder architecture with attention mechanisms processes input sequences into context vectors, then generates output sequences while attending to relevant input positions. This enables effective sequence-to-sequence learning for translation and summarization tasks.
The gradient descent update rule w_new = w_old - learning_rate * gradient minimizes the loss function by moving parameters in the direction of steepest descent. The learning rate controls step size, requiring careful tuning to balance convergence speed and stability. In contrast, the learning rate schedule determines how the step size changes during training. common strategies include step decay, exponential decay, and cosine annealing. proper scheduling enables initial rapid learning followed by fine-tuning for optimal convergence.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. In contrast, dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. at test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization.
The universal approximation theorem states that a feedforward network with a single hidden layer containing a finite number of neurons can approximate any continuous function on compact subsets of n-dimensional space, given appropriate activation functions and sufficient neurons. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: Explain how residual connections enable training of very deep networks.
A: Residual connections add skip connections that allow gradients to flow directly through the network via identity mappings. This creates shorter gradient paths, mitigating vanishing gradients and enabling effective training of networks with hundreds of layers.
Q: Explain this in detail.
A: Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise.
Weight decay implements L2 regularization by adding a penalty term proportional to the squared magnitude of weights to the loss function. This encourages smaller weights, reducing model complexity and improving generalization by preventing overfitting to training noise. Furthermore, the jacobian matrix contains all first-order partial derivatives of a vector-valued function. in backpropagation, jacobians represent how layer outputs change with respect to inputs, enabling gradient computation through matrix multiplication via the chain rule.
Gradient accumulation simulates larger batch sizes by accumulating gradients over multiple forward-backward passes before updating parameters. This enables training with effective batch sizes exceeding memory constraints while maintaining gradient stability. Q: What is the difference between batch, layer, and instance normalization?
A: Batch normalization normalizes across the batch dimension for each feature. Layer normalization normalizes across all features for each sample. Instance normalization normalizes each feature map independently. The choice depends on batch size stability and data type.
Batch normalization reduces internal covariate shift by normalizing layer inputs to have zero mean and unit variance. It also acts as a regularizer, adds noise through batch statistics, and enables higher learning rates by preventing activation saturation. Furthermore, second-order optimization methods like newton's method use curvature information from the hessian matrix to determine optimal step sizes and directions. while theoretically superior, computational cost limits practical application to small-scale problems.
Dropout can be interpreted as training an ensemble of exponentially many sub-networks that share parameters. At test time, using all weights scaled by the dropout probability approximates averaging predictions from this ensemble, improving generalization. Q: How does weight initialization affect neural network training?
A: Proper initialization maintains activation and gradient variance across layers. Xavier initialization scales weights by the square root of fan-in for sigmoid/tanh. He initialization uses fan-in for ReLU. Poor initialization causes vanishing or exploding activations, preventing effective learning.
